{
  "name": "pda",
  "workspaces": [
    "packages/client",
    "packages/relay",
    "packages/visual"
  ],
  "scripts": {
    "client": "npm run dev -w client -- --host",
    "client:build": "npm run build -w client",
    "relay": "npm run start -w relay",
    "visual": "npm run dev -w visual -- --host",
    "visual:build": "npm run build -w visual"
  }
}
