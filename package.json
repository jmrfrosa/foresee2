{
  "name": "pda",
  "workspaces": [
    "packages/client",
    "packages/relay",
    "packages/visual"
  ],
  "scripts": {
    "client:dev": "npm run dev -w client -- --host",
    "client:build": "npm run build -w client",
    "relay:dev": "npm run dev -w relay",
    "relay:build": "npm run build -w relay",
    "relay:start": "npm run start -w relay",
    "visual:dev": "npm run dev -w visual -- --host",
    "visual:build": "npm run build -w visual"
  }
}
